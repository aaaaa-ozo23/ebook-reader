{
  "name": "ebook-reader",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=26.1.0",
    "pnpm": ">=11.1.2"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
